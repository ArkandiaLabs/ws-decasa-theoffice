# Generado por el build o por npm; formatearlo no aporta y ensucia los diffs.
/dist
/.angular
/coverage
# Lo genera pnpm; formatearlo solo produce ruido en los diffs.
pnpm-lock.yaml
# Los genera `make web-tokens` desde DESIGN.md; formatearlos los sacaria de sincronia.
/design/tokens.json
/src/theme.css
