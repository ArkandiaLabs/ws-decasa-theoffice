<!--
Placeholders:
  {{PROJECT_NAME}}
  {{SOLUTION_FILES}}        — .sln / .slnx file name(s)
  {{TARGET_FRAMEWORKS}}     — e.g. "net10.0 (api, core), net10.0-android/-ios (mobile)"
  {{LANG_POSTURE}}          — e.g. "C# 14, nullable enabled, implicit usings"
  {{PACKAGE_MANAGEMENT}}    — e.g. "central (Directory.Packages.props)" or "per-project"
  {{EF_PROVIDER}}           — e.g. "SQL Server (EF Core 10)" or "none"
  {{DI_CONTAINER}}          — e.g. "built-in (Program.cs)" or "Autofac"
  {{SECRETS_STRATEGY}}      — e.g. "user-secrets (dev) + Azure Key Vault (prod)"
  {{TEST_RUNNER}}           — e.g. "Microsoft.Testing.Platform (global.json)" or "VSTest (default)"
  {{PACKAGING_SHAPE}}       — e.g. "Dockerfile" / "SDK container publishing" / "framework-dependent publish"
Keep this file to .NET-shaped facts the generic docs can't hold. Do NOT restate architecture.md.
Leave <!-- TODO --> where you couldn't read the answer. DELETE sections that don't apply —
a deleted section beats a section full of TODOs.
-->

# .NET Solution Guide — {{PROJECT_NAME}}

Deep .NET context for AI coding agents. For the general picture see [architecture](./architecture.md),
[data model](./data-model.md), and [infrastructure](./infrastructure.md).

## 1. Solution & projects

- **Solution file(s):** {{SOLUTION_FILES}}

| Project | Type | TFM | Purpose |
|---|---|---|---|
| <!-- Acme.Api --> | <!-- Web API --> | <!-- net10.0 --> | <!-- one line --> |
| | | | |

**Project references** (layering):

```mermaid
flowchart LR
  api[Acme.Api] --> core[Acme.Core]
  core --> infra[Acme.Infrastructure]
```

<!-- TODO: replace with the real project-reference graph. -->

## 2. Target frameworks & language

- **Target framework(s):** {{TARGET_FRAMEWORKS}}
- **Language posture:** {{LANG_POSTURE}}
- **Pinned SDK / shared build props:** <!-- TODO: global.json, Directory.Build.props if any -->

## 3. Package management & dependencies

- **Version management:** {{PACKAGE_MANAGEMENT}}
  <!-- If central: PackageReference in a .csproj must NOT carry a Version attribute — versions live
       in Directory.Packages.props as <PackageVersion>. Keep this line; agents get it wrong. -->
- **Feeds:** <!-- TODO: nuget.config sources, incl. private feeds. Delete if only nuget.org. -->
- **Local tools:** <!-- TODO: .config/dotnet-tools.json — if present, commands are `dotnet tool restore` then `dotnet <tool>`. Delete if absent. -->

| Package | Version | Role |
|---|---|---|
| <!-- Microsoft.EntityFrameworkCore.SqlServer --> | | <!-- ORM --> |
| | | |

- **Dependency health:** <!-- TODO: note version freshness / known-vuln risk only if you have a concrete signal. -->

## 4. Aspire orchestration

<!-- DELETE this whole section if there is no AppHost project. -->

- **AppHost:** <!-- TODO: project + file; what resources it composes (projects, DB, cache, queue) -->
- **ServiceDefaults:** <!-- TODO: what AddServiceDefaults() wires up — OTel, health checks, service discovery, HttpClient resilience -->
- **Run the system with:** <!-- `aspire run` or `dotnet run --project <…>.AppHost` — NOT each service individually -->
- **Convention:** ServiceDefaults holds shared cross-cutting wiring only — do not put models or business logic there.

## 5. Composition root / DI

- **Container:** {{DI_CONTAINER}}
- **Registration lives in:** <!-- TODO: Program.cs / Startup.cs / *Extensions / Autofac modules -->
- **Conventions / notable lifetimes:** <!-- TODO: scoped DbContext, hosted services, options pattern, assembly scanning... -->

## 6. Data access (EF Core)

- **Provider:** {{EF_PROVIDER}}
- **DbContext:** <!-- TODO: class + path. Flag if very large. -->
- **Model configuration:** <!-- TODO: data annotations / Fluent API / IEntityTypeConfiguration -->
- **Query filters:** <!-- TODO: global or named query filters (tenant scoping, soft delete) and whether any can be disabled by name. Delete if none. -->
- **Migrations:** <!-- TODO: path + how applied (dotnet ef / Migrate() on startup / CI). Cross-link data-model.md -->

## 7. Configuration & secrets

- **Config layering:** <!-- TODO: appsettings.json -> appsettings.{Env}.json -> user-secrets/env -> Key Vault -->
- **Secrets strategy:** {{SECRETS_STRATEGY}}
- **⚠ Committed secrets:** <!-- TODO: list any secret found committed (do NOT paste the value); recommend user-secrets / Key Vault + appsettings.example.json. Delete this line if none. -->

## 8. Build, run, test

```bash
# TODO: keep the 3-6 commands developers actually run
dotnet build
dotnet run --project <main project>
dotnet test
dotnet ef migrations add <Name> --project <db project>
```

- **Test runner:** {{TEST_RUNNER}}
- **Test framework(s):** <!-- TODO: xUnit / NUnit / MSTest -->
- **Supporting libraries:** <!-- TODO: mocking, assertions, integration (WebApplicationFactory / Testcontainers), coverage -->
- **Test organization:** <!-- TODO: by layer / by domain / integration vs unit vs automation -->

## 9. Quality gates

| Gate | Status | Notes |
|---|---|---|
| `.editorconfig` | <!-- present/absent --> | |
| Roslyn analyzers (StyleCop / NetAnalyzers) | <!-- present/absent --> | |
| Architecture linting (NsDepCop / ArchUnitNET) | <!-- present/absent --> | |
| Warnings as errors | <!-- present/absent --> | |

<!-- TODO: where absent, recommend adopting it to enforce the layering in §1. -->

## 10. UI & API surface

<!-- TODO: keep only what applies; DELETE if headless with no HTTP surface. -->

- **API shape:** <!-- minimal APIs / controllers; grouping; validation approach -->
- **OpenAPI:** <!-- built-in Microsoft.AspNetCore.OpenApi (3.1 by default in .NET 10; WithOpenApi() is deprecated) / Swashbuckle / NSwag / Scalar -->
- **Identity:** <!-- ASP.NET Core Identity (passkeys?) / Entra ID / IdentityServer / JWT bearer -->
- **UI stack:** <!-- Razor Pages / MVC / Blazor (render modes) / MAUI -->
- **Platform-specific wiring (MAUI):** <!-- Platforms/Android & Platforms/iOS services, handlers, deep links -->

## 11. Deployment & packaging

- **Packaging shape:** {{PACKAGING_SHAPE}}
  <!-- Note: the SDK can build container images with NO Dockerfile via `dotnet publish /t:PublishContainer`
       and <ContainerRepository> / <ContainerImageFormat>. Don't infer "not containerized" from a missing Dockerfile. -->
- **AOT / trimming / single file:** <!-- TODO: PublishAot, PublishTrimmed, TrimMode, PublishSingleFile, InvariantGlobalization. Delete if none. -->
  <!-- If any are on, they are hard constraints: no unbounded reflection, no reflection-based JSON — use source-generated JsonSerializerContext. Repeat in §13. -->
- **Output layout:** <!-- TODO: ArtifactsPath if set; otherwise default bin/obj. Delete if default. -->

## 12. Cross-cutting concerns

<!-- DELETE if the repo has none of these. -->

- **Observability:** <!-- OpenTelemetry, health checks, Serilog/NLog -->
- **Resilience:** <!-- Microsoft.Extensions.Http.Resilience / Polly pipelines -->
- **AI integrations:** <!-- Microsoft.Extensions.AI, Semantic Kernel, ModelContextProtocol, .mcp.json -->

<!-- If Aspire is present (§4), note that ServiceDefaults likely already supplies these — extend it, don't re-register per service. -->

## 13. Gotchas / hotspots

<!-- TODO: the non-obvious things that trip up an agent editing this solution. Examples:
- DbContext is ~9k LOC — split changes carefully.
- Service methods are async but don't thread CancellationToken — follow the existing pattern.
- PublishAot is on: no reflection-based serialization.
- Project X must not reference project Y (see ADRs).
- The codebase uses explicit backing fields, not the C# 14 `field` keyword — match it.
-->

## 14. File-based apps

<!-- DELETE unless the repo has standalone .cs scripts (#:package / #:sdk / shebang). -->

<!-- TODO: list them. They run with `dotnet run <file>.cs`, target native AOT by default in .NET 10,
     and can be promoted with `dotnet project convert`. -->

## Related docs

- [Architecture](./architecture.md) · [Data model](./data-model.md) · [Infrastructure](./infrastructure.md) · [Decisions](./adrs/)
<!-- Add any pre-existing repo docs here rather than restating them. -->
