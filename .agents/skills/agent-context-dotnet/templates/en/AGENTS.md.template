<!--
Placeholders:
  {{PROJECT_NAME}}         — e.g. "Campus Prep"
  {{ONE_LINE_PURPOSE}}     — what this product does in one sentence
  {{DOC_LINKS}}            — bulleted list of doc pointers
  {{COMMANDS_BLOCK}}       — code block with the 3-5 most common dev commands
  {{NON_OBVIOUS_RULES}}    — user-supplied rules from the interview
  {{TESTING_NOTES}}        — one paragraph on how tests are run
  {{CODE_STYLE_NOTES}}     — one paragraph on linters/formatters in use
-->

# AGENTS.md — {{PROJECT_NAME}}

{{ONE_LINE_PURPOSE}}

Guidance for AI coding agents working in this repo. Follows the [agents.md](https://agents.md) convention.

This file only captures what isn't obvious from the code. For architecture, data model, decisions, and broader context, follow the pointers below and read the source.

## Where to find things

{{DOC_LINKS}}

Read these before making structural changes.

## Commands

{{COMMANDS_BLOCK}}

## Non-obvious rules

{{NON_OBVIOUS_RULES}}

## Testing

{{TESTING_NOTES}}

## Code style

{{CODE_STYLE_NOTES}}

## Security

- Do not commit `.env` or any credentials file. Add new variables to `.env.example`.
- Do not log secrets, tokens, or personally identifiable information.
- Assume anything in this repo is readable by an AI agent — never paste secrets here.
